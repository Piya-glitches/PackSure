# CPU-only PyTorch build. Without this, `pip install torch` pulls the full
# CUDA-enabled build (800MB+) regardless of whether a GPU is present -- and
# every model in this pipeline runs with gpu=False. This index serves
# CPU-only wheels at a fraction of the size/download time.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# Layer 0 — quality gate + barcode calibration
opencv-python-headless==4.10.0.84
pyzbar==0.1.9
python-barcode==0.15.1
Pillow==10.4.0
numpy==1.26.4

# Layer 1 — PDP localization (YOLOv8)
ultralytics==8.3.10

# Layer 2 — CRAFT text detection + CRNN recognition (EasyOCR ships both,
# pretrained; see app/pipeline/text_detection_ocr.py for why this is a
# genuine match for the architecture rather than a substitution)
easyocr==1.7.2
torch==2.4.1+cpu
torchvision==0.19.1+cpu

# Layer 3 — DistilBERT/IndicBERT NER backbone
transformers==4.45.1

# Layer 5 — reporting
reportlab==4.2.5
